package com.example.aistudy.data.models
enum class Priority(val emoji: String) {
    HIGH( "💥"), // Changed emoji
    MEDIUM("🌟"), // Changed emoji
    LOW("🍃"), // Changed emoji
    NONE("🌬️") // Changed emoji
}

